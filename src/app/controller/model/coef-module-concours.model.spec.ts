import { CoefModuleConcours } from './coef-module-concours.model';

describe('CoefModuleConcours', () => {
  it('should create an instance', () => {
    expect(new CoefModuleConcours()).toBeTruthy();
  });
});
